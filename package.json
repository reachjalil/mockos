{
  "name": "@mockos/workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Mock identity infrastructure for integration tests.",
  "type": "module",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.30.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "brand:check": "node scripts/render-brand-assets.mjs --check",
    "brand:render": "node scripts/render-brand-assets.mjs",
    "build": "turbo run build",
    "check": "pnpm format:check && pnpm lint && pnpm docs:check && pnpm m6:parity:check && pnpm f0:drift:check && pnpm f0:guards:check && pnpm brand:check && pnpm public-boundary:check && pnpm types && pnpm test && pnpm build && pnpm worker:check",
    "dev": "pnpm --filter @mockos/worker dev",
    "docs:check": "node scripts/check-doc-status.mjs && node scripts/check-doc-links.mjs && node scripts/check-mcp-docs.mjs",
    "e2e:entra-msal": "node scripts/e2e-entra-msal.mjs",
    "e2e:entra-msal-cleanup": "node scripts/e2e-entra-msal-cleanup.mjs",
    "e2e:provisioning": "node scripts/e2e-provisioning.mjs",
    "f0:drift:check": "tsx scripts/generate-f0-artifacts.ts --check",
    "f0:generate": "tsx scripts/generate-f0-artifacts.ts",
    "f0:guards:check": "node scripts/check-f0-guards.mjs",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "m6:parity:check": "node scripts/generate-m6-parity.mjs --check",
    "m6:parity:generate": "node scripts/generate-m6-parity.mjs",
    "m6:parity:test": "node --test scripts/generate-m6-parity.test.mjs",
    "public-boundary:check": "node scripts/check-public-dependencies.mjs",
    "smoke:test": "node --test scripts/smoke-worker-helpers.test.mjs",
    "test": "pnpm m6:parity:test && pnpm smoke:test && turbo run test",
    "types": "turbo run types",
    "worker:check": "node scripts/check-worker-wrangler.mjs --allow-workers-dev",
    "worker:dry-run": "pnpm --filter @mockos/worker exec wrangler deploy --dry-run --env= --outdir .wrangler/dry-run"
  },
  "devDependencies": {
    "@azure/msal-node": "catalog:",
    "@biomejs/biome": "catalog:",
    "@changesets/cli": "catalog:",
    "@modelcontextprotocol/sdk": "catalog:",
    "@types/node": "catalog:",
    "sharp": "catalog:",
    "turbo": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "pnpm": {
    "overrides": {
      "sharp": "0.35.3"
    },
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "workerd"
    ]
  }
}
