---
"@mockos/worker-kit": minor
---

Expose a routing-only package entrypoint so consumers can compose and test the
environment router without evaluating Worker-only Durable Object, Workflow, or
Agents modules.
