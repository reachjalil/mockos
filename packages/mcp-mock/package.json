{
  "name": "@mockos/mcp-mock",
  "version": "0.0.0",
  "description": "SDK-independent MCP data-plane adapter for mockOS environments.",
  "type": "module",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "types": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mockos/contracts": "workspace:*",
    "@mockos/core": "workspace:*"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "catalog:",
    "@mockos/testkit": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
