# mockos

This is the temporary npm package-name reservation for
[mockOS](https://github.com/reachjalil/mockos), mock identity infrastructure for
integration tests.

Version `0.0.1` contains only reservation metadata. It is not the mockOS runtime,
CLI, hosted service, or a stable public API. Follow the public repository for
implementation status and installation guidance.
