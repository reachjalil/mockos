# 🥸 @mockos/cli

**Package-name reservation only.**

This is the temporary CLI package reservation for
[mockOS](https://github.com/reachjalil/mockos), mock identity infrastructure for
integration tests.

Version `0.0.1` contains only reservation metadata. It is not a functional CLI,
hosted service, or stable public API. Follow the public repository for
implementation status and installation guidance.
