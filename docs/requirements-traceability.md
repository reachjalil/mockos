# Requirements traceability

Status: M0-M3 accepted; M5 provisioning source is locally qualified pending immutable-revision, hosted-CI, and deployment acceptance
Last reviewed: 2026-07-22

States are Complete, Partial, Not started, Blocked, or Intentionally deferred.
“Complete for M3 scope” means the bounded implemented requirement passed its local and
hosted gates plus the applicable deployed acceptance sample; it does not imply broad
or live-provider parity. Exact revision `8645f405d5e3b922c30d51339b8b27f9fe30d93e`
passed [hosted CI](https://github.com/reachjalil/mockos/actions/runs/29886610480)
and the [staging/production M3 smoke](./evidence/m3-workers-dev-smoke.md).
“Partial for M5” means source and local qualification evidence exist in the current
source candidate; it does not inherit the accepted M3 revision, CI run, or deployment
record. The [M5 local source record](./evidence/m5-local-source-qualification.md)
captures the green full gate and two-process provisioning e2e.

| ID | Requirement | State | Evidence / next gate |
| --- | --- | --- | --- |
| IDP-ENTRA-001 | Tenant-scoped OIDC discovery | Complete for M1; live-reverified in M2 | [fixture](../packages/testkit/fixtures/entra/oidc/01-discovery.json), [Worker integration](../apps/worker/test/oidc.integration.test.ts), and [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| IDP-ENTRA-002 | Authorization code with PKCE S256 | Complete for M1; live-reverified in M2 | [fixture](../packages/testkit/fixtures/entra/oidc/10-authorize-pkce-s256.json), [core test](../packages/core/src/core.test.ts), [Worker integration](../apps/worker/test/oidc.integration.test.ts), and [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| IDP-ENTRA-003 | Provider-shaped AADSTS errors | Complete for M2 catalog slice | [Core error-catalog test](../packages/core/src/core.test.ts), documented fixtures, [Worker scenario integration](../apps/worker/test/mcp.integration.test.ts), and live AADSTS50076 evidence in the [deployed smoke](./evidence/m2-workers-dev-smoke.md); corpus-wide live-provider parity remains unverified |
| IDP-OKTA-001 | Okta OIDC profile and errors | Complete for M2 slice locally | 22 [source-reviewed fixtures](../packages/testkit/fixtures/okta/oidc), [core](../packages/core/src/okta.test.ts), [HTTP adapter](../packages/engine-http/src/okta.test.ts), and [Worker integration](../apps/worker/test/okta.integration.test.ts); live-tenant and deployed-smoke comparison remain open |
| SCIM-001 | RFC 7643 resource schemas and discovery | Complete for M3 scope | Portable [contracts](../packages/contracts/src/scim.ts), discovery routes in the [SCIM adapter](../packages/engine-http/src/scim.ts), the [Worker mount](../apps/worker/test/scim.integration.test.ts), and the RFC fixture corpus cover ServiceProviderConfig, ResourceTypes, core User/Group schemas, and the enterprise User extension. Both deployed provider profiles passed ServiceProviderConfig discovery; no live-provider comparison is claimed |
| SCIM-002 | RFC 7644 CRUD, filter, PATCH, pagination, and ETag | Complete for the bounded M3 scope | Versioned [directory repositories](../packages/core/src/directory), bounded [filter](../packages/core/src/scim/filter.ts) and [PATCH](../packages/core/src/scim/patch.ts) code, adapter tests, and all 91 [RFC fixtures](../packages/testkit/fixtures/rfc/scim) execute green locally and in hosted CI. The deployed sample passed Group PATCH persistence and ETag checks for both profiles; it did not run all fixtures remotely |
| SCIM-003 | Entra and Okta inbound SCIM dialects | Complete for M3 scope | Provider profiles select case-insensitive and filtered Entra PATCH shapes with optional Group `204`, and Okta PUT-heavy/pathless/filtered membership shapes with Group `200`; all 10 [Entra](../packages/testkit/fixtures/entra/scim) and 12 [Okta](../packages/testkit/fixtures/okta/scim) fixtures execute green locally and in hosted CI, while deployed discovery/PATCH passed for both. Live-provider parity is not claimed |
| DIR-001 | Microsoft Graph directory reads | Complete for the bounded M3 read scope | The [Graph adapter](../packages/engine-http/src/graph.ts), [adapter tests](../packages/engine-http/src/graph.test.ts), portable [composition seam](../packages/worker-kit/src/directory-http.ts), and Worker integration cover bounded User/Group reads, membership reads, selection, filters, and cursor pages. The deployed smoke sampled a projected User read; Graph writes and broad parity are not claimed |
| DIR-002 | Okta Users, Groups, memberships, and lifecycle API | Complete for the bounded M3 scope | The [Okta directory adapter](../packages/engine-http/src/okta-api.ts), [adapter tests](../packages/engine-http/src/okta-api.test.ts), portable [composition seam](../packages/worker-kit/src/directory-http.ts), and Worker integration cover bounded Users, Groups, memberships, pages, and lifecycle actions. The deployed smoke sampled an active User read and `E0000047`; Classic Authn and broad parity are not implemented |
| LIFE-001 | Provider lifecycle state machine and credential/membership cascade | Complete for M3 scope | [Lifecycle policy](../packages/core/src/directory/lifecycle.ts), [core tests](../packages/core/src/directory-lifecycle.test.ts), and the [Worker cascade integration](../apps/worker/test/lifecycle-cascade.integration.test.ts) cover Entra activate/disable/reactivate/delete, Okta activate/reactivate/suspend/unsuspend/deprovision/delete, transactional access/refresh revocation, stable no-ops, and User-delete membership removal with affected Group version bumps. Deployed acceptance sampled Entra disable, version advance, revocation, and AADSTS50057; Okta lifecycle remains local/hosted evidence |
| RBAC-001 | App roles and assignments | Partial | Storage contracts may exist; token-claim integration required |
| RBAC-002 | Group claims and overage | Not started | M6 |
| MAP-001 | Entra/Okta profile mapping without engine forks | Complete for M3 scope | Shared engine provider profiles in [core](../packages/core/src/providers) select inbound SCIM PATCH/status behavior and provider lifecycle policy. The local/hosted suite is green, and deployed SCIM PATCH returned the configured Entra `204` and Okta `200`; live-provider qualification remains open |
| TOK-001 | RS256 signing and JWKS validation | Complete for M2 scope | [Core crypto test](../packages/core/src/core.test.ts), Worker round trips, and both minted-token and hosted-flow signature checks in the [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| TOK-002 | Hashed rotating refresh-token families | Complete for M3 scope | [OAuth core](../packages/core/src/oauth/oauth-service.ts) and [tests](../packages/core/src/okta.test.ts) cover hashed tokens, atomic single-use rotation, absolute family expiry/auth-time preservation, scope narrowing, rollback on persistence failure, concurrent redemption, replay-family revocation, and associated access-token revocation. Entra and Okta Worker paths pass locally/hosted; deployed acceptance sampled Entra rotation, narrowing, signed identity, lifecycle revocation, and rejected reuse after disable |
| TEN-001 | One isolated SQLite store per environment | Complete for M2 | [Node adapter test](../packages/testkit/src/testkit.test.ts), Durable Object [MCP isolation integration](../apps/worker/test/mcp.integration.test.ts), and empty-catalog cleanup in the [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| TEN-002 | Derive issuer per request; never persist absolute issuer | Complete for M2 | [Core persistence invariant](../packages/core/src/core.test.ts), request-host assertions in Worker integrations, and distinct staging/production issuer verification in the [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| TEN-003 | Path and subdomain host resolution | Complete in unit scope | [host resolver tests](../packages/worker-kit/src/host-resolver.test.ts); subdomain live TLS/routing verification remains M8 |
| SCN-001 | Deterministic delay/error/mutation injection | Complete for M2 | [Core deterministic/cap tests](../packages/core/src/scenario-log.test.ts), [Worker integration](../apps/worker/test/mcp.integration.test.ts), and one-shot live error injection in the [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| MCP-001 | Handler-agnostic tool registry | Complete for M3; partial for M5 | The accepted 14-tool [registry and tests](../packages/mcp/src/index.test.ts) include `simulate_lifecycle`. The M5 source candidate adds `run_provisioning_cycle` as tool 15; registry tests, its authenticated Worker/Workflow mount, the full gate, and the built-CLI process e2e are green locally. Hosted CI and deployed smoke remain pending |
| MCP-002 | Session current-environment cursor | Complete for M2 | [Registry cursor tests](../packages/mcp/src/index.test.ts) and mounted-session [Worker integration](../apps/worker/test/mcp.integration.test.ts) |
| AUDIT-001 | Inbound/outbound request log and assertions | Partial M5 source candidate | The accepted engine supports inbound/control entries, filtering, pagination, retention, and assertions. M5 [core tests](../packages/core/src/scenario-log.test.ts) add response-body predicates and repeated non-overlapping ordered sequences, while worker-kit adds bounded/redacted outbound capture. Worker/full local evidence is green, and the process e2e matches the expected four-step outbound sequence; deployed evidence remains pending |
| AUDIT-002 | Redact platform and target secrets while retaining synthetic protocol bodies | Complete for M2 platform-key boundary; partial for M5 target-key boundary | API-key redaction is accepted through M3. M5 [outbound HTTP tests](../packages/worker-kit/src/provisioning-http.test.ts), Worker tests, and the process e2e cover credential-scoped headers, reflected-secret redaction, and target capture redaction. Exact active self-host `API_KEY` reuse is rejected regardless of prefix, and a later key-rotation collision fails before outbound execution. Hosted/deployed security evidence remains pending |
| PROV-001 | Deterministic outbound SCIM planner and response interpreter | Partial M5 source candidate | The portable [contracts](../packages/contracts/src/provisioning.ts) and [core planner/interpreter](../packages/core/src/provisioning) model full/incremental snapshots, Entra filtered PATCH and deactivate/delete behavior, Okta PUT/group-push behavior, watermarks, lookups, and explicit rate-limit waits/retries. Focused tests and the full local repository gate are green; deployed gates remain pending |
| PROV-002 | SSRF guard at save and fetch time | Partial M5 source candidate | [Secure fetch](../packages/worker-kit/src/secure-fetch.ts) validates at ingress/fetch, requires HTTPS by default, blocks private/special literal and product hosts, disables redirects, and bounds time/bodies. Worker/worker-kit tests and the full local gate are green; Workers cannot pin DNS answers, and hosted/deployed qualification remains pending |
| PROV-003 | Durable provisioning-cycle execution and active-start reconciliation | Partial M5 source candidate | The [Workflow entrypoint](../packages/worker-kit/src/provisioning-workflow.ts), environment-Durable-Object persistence, target isolation, batching, execution ordinals, watermarks, fixed run IDs, and active-run recovery test cases are present. Request-level replay after a terminal result is explicitly deferred to F4. The real two-process Worker/Workflow e2e and full gate are green locally; hosted CI and deployed controlled-target smoke remain pending |
| PROV-004 | Deterministic SCIM target test surface and sequence assertion | Partial M5 source candidate | The Durable Object-backed [target-app example](../examples/target-app) records SCIM requests, and the core assertion checks ordered request/response shapes and repeated non-overlapping sequences. The fresh two-process e2e proves four matched requests, one target User, one target Group, credential redaction, and clean teardown. Direct loopback remains blocked; deployed public-HTTPS target evidence is pending |
| OPS-001 | CI format/types/test/build + Wrangler gates | Complete for M3; M5 local gate green | The full local M5 `pnpm check` passes format, lint, docs, brand, public-boundary, types, all tests, build, Worker checks, and production/staging dry runs. The accepted M3 gates are [green in hosted CI](https://github.com/reachjalil/mockos/actions/runs/29886610480) at exact candidate `8645f405d5e3b922c30d51339b8b27f9fe30d93e`; no immutable M5 revision or hosted-CI run is recorded yet |
| OPS-002 | workers.dev deploy and smoke | Complete for M3 manual acceptance | Exact candidate, deployment/version IDs, expanded authenticated flow, reverse cleanup, and empty catalogs are recorded in the [M3 deployed smoke evidence](./evidence/m3-workers-dev-smoke.md). The [deploy-workflow run](https://github.com/reachjalil/mockos/actions/runs/29886659744) was skipped; deployment is disabled and still lacks its Cloudflare API token, so automated execution is not qualified |
| CLI-001 | Non-interactive operator loop | Complete through M3; partial M5 provisioning command | Profiles, diagnostics, environment lifecycle, seeding, applications, scenarios, minting, log/assertion output, JUnit, well-known URLs, reports, and capability negotiation pass accepted source tests. The M5 `provision run` candidate accepts target secrets from a file or standard input, not argv; all 19 [CLI tests](../packages/cli/test/cli.test.ts) and the built-CLI process e2e are green locally. It rejects `mk_` keys and an exact active non-prefixed self-host Access Key before calling MCP. Publication and deployed command-matrix evidence remain open |
| OPS-003 | Custom-domain cutover | Blocked | Domain purchase and M8 runbook execution |
| SAML-001 | SAML protocol | Intentionally deferred | v2 product scope |
