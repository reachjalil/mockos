# Requirements traceability

Status: Accepted M0-M3/M5 and sampled M6 retained; M7/F0/F1 have source evidence, F2
has partial OpenAI/Anthropic provider and metadata-observation source evidence, and
bounded Entra MSAL Node authorization-code/public-device and Okta Auth JS local X/Q passed
Last reviewed: 2026-07-26

States are Complete, Partial, Not started, Blocked, or Intentionally deferred.
“Complete for M3 scope” means the bounded implemented requirement passed its local and
hosted gates plus the applicable deployed acceptance sample; it does not imply broad
or live-provider parity. Exact revision `8645f405d5e3b922c30d51339b8b27f9fe30d93e`
passed [hosted CI](https://github.com/reachjalil/mockos/actions/runs/29886610480)
and the [staging/production M3 smoke](./evidence/m3-workers-dev-smoke.md).
M5 evidence is bound separately to public revision
`ac8d6d1b29003b7e9a9087d33c3dc2c4c3d55a93`,
[CI run 29957994237](https://github.com/reachjalil/mockos/actions/runs/29957994237),
the [local source record](./evidence/m5-local-source-qualification.md), and the
[manual deployment/hosted acceptance record](./evidence/m5-workers-dev-smoke.md).
That record does not qualify either repository's guarded promotion workflow. M6 is
bound separately to exact public revision
`a01fb6abbaf85e2cd98b42a3839bebe7451cf8da`,
[CI run 29966667984](https://github.com/reachjalil/mockos/actions/runs/29966667984),
and the [staging/production sampled smoke](./evidence/m6-workers-dev-smoke.md).
“Partial for M6” still means the named product capability is bounded or incomplete;
the linked deployed sample does not promote unexercised cases or fixture corpora.
The M7 management-read entries are bound only to the source and local tests in the
repository revision carrying this document. They have no hosted-CI, deployed-version,
private-console, or browser-acceptance evidence yet.
The F0 entries likewise mean the complete local gate is green for this source
candidate; they do not imply hosted CI, merge, deployment, package publication, or
activation of an experimental runtime.
The F1 entries bind to the revision carrying this document, whose focused suites and
complete local repository gate are green. They do not inherit M5/M6 hosted CI or
deployment evidence and do not qualify merge, private Cloud composition, wildcard
TLS, package publication, a broader client/ecosystem matrix, or a production service.
The built-in `salesforce/hosted-mcp/sobject-reads` entry additionally binds only to
documentation provenance reviewed on 2026-07-25, deterministic source fixtures, and a
mounted official MCP SDK `1.29.0` Worker exercise/observe/assert loop. That establishes
D/I/S/X/Q locally, not actual-network, Salesforce REST/OAuth/field-level
security/sharing/output-wire parity, hosted, deployed, Cloud-pin, H, V, or P evidence. The
built-in server-definition catalog is not the portable F5 export/import/apply/gallery
system.
The F2 entries bind neutral plan, behavior-adapter, pure renderer, strict
server-definition, repository/migration, MCP, bounded OpenAI/Anthropic request/runtime,
metadata-only observation, and local Worker official-SDK/MCP tests to the source
carrying this document. They
establish MCP-only definition management plus bounded model list/retrieve, OpenAI
Chat Completions and Anthropic Messages as JSON or timed SSE, plus fail-open exact
request-log query/count/ordered-sequence matching for successfully parsed/planned,
response-preflighted POSTs. They do
not establish configured midstream errors, the OpenAI Responses
API, runtime/conversation state, reset, audit-complete observation,
Wrangler-network or hosted qualification, private Cloud integration, or complete F2.

Evidence levels are independent: designed (D), implemented (I), source-tested (S),
integration-tested (X), pinned SDK/client-qualified (Q), hosted-smoke (H),
verified-live (V), and production-ready (P). The
[documentation index](./README.md) defines each boundary. The Entra MSAL Node and Okta
Auth JS requirements are each D/I/S/X/Q yes and H/V/P no. No requirement currently
has V or P.

| ID | Requirement | State | Evidence / next gate |
| --- | --- | --- | --- |
| IDP-ENTRA-001 | Tenant-scoped OIDC discovery | Complete for M1; deployed-reverified in M2; truthful UserInfo omission source-tested | [fixture](../packages/testkit/fixtures/entra/oidc/01-discovery.json), [Worker integration](../apps/worker/test/oidc.integration.test.ts), MCP management tests, and [deployed mock smoke](./evidence/m2-workers-dev-smoke.md). Current OIDC discovery and `get_wellknown_urls` omit UserInfo because no route exists; fixtures 28 and 29 remain documented-only, so the omission adds no UserInfo support or evidence tier. |
| IDP-ENTRA-002 | Authorization code with PKCE S256 | Complete for M1; deployed-reverified in M2 | [fixture](../packages/testkit/fixtures/entra/oidc/10-authorize-pkce-s256.json), [core test](../packages/core/src/core.test.ts), [Worker integration](../apps/worker/test/oidc.integration.test.ts), and [deployed mock smoke](./evidence/m2-workers-dev-smoke.md) |
| IDP-ENTRA-003 | Provider-shaped AADSTS errors | Complete for M2 catalog slice | [Core error-catalog test](../packages/core/src/core.test.ts), documented fixtures, [Worker scenario integration](../apps/worker/test/mcp.integration.test.ts), and an AADSTS50076-shaped emulator response in the [deployed mock smoke](./evidence/m2-workers-dev-smoke.md); verified-live catalog comparison remains open |
| IDP-ENTRA-004 | Pinned MSAL Node custom-authority code + S256 PKCE, forced refresh, lifecycle rejection, and bounded harness cleanup | Bounded local D/I/S/X/Q complete; H/V/P open | The [actual-network harness](../scripts/e2e-entra-msal.mjs), [official-client flow](../scripts/e2e-entra-msal-client.mjs), [signal cleanup probe](../scripts/e2e-entra-msal-cleanup.mjs), and [local evidence](./evidence/entra-msal-node-local-qualification.md) pin `@azure/msal-node` 5.4.2 and `@modelcontextprotocol/sdk` 1.29.0. The official MCP client creates/configures/observes/cleans up the environment; MSAL uses returned authority, host-only `knownAuthorities`, and `ProtocolMode.OIDC`, completes code + S256 PKCE and a forced refresh over local Wrangler HTTPS, then surfaces `invalid_grant`/`AADSTS50057` after disable. Normal cleanup passes, and the focused ready-Worker `SIGTERM` probe observes exit `143`, released port, absent process group, and removed temporary state. `SIGKILL`, Windows process-tree behavior, other grants/aliases, browser/Graph SDK, hosted Cloud, real Entra, and production readiness remain outside the requirement. |
| IDP-ENTRA-005 | Entra public-client device authorization, credential-gated activation, atomic redemption, refresh/lifecycle, and pinned MSAL Node path | Bounded local D/I/S/X/Q complete; H/V/P open | The [core suite](../packages/core/src/device-authorization.test.ts), [core-backed fixture executor](../packages/engine-http/src/entra-device-fixtures.test.ts), [mounted Worker integration](../apps/worker/test/entra-device.integration.test.ts), [official-client flow](../scripts/e2e-entra-msal-client.mjs), [quickstart](./quickstarts/entra-msal-node.md), and [local evidence](./evidence/entra-msal-node-local-qualification.md) cover public-only start at `POST /<tenant>/oauth2/v2.0/devicecode`, 900-second/five-second policy without `verification_uri_complete`, authenticated approve and deny at `/devicelogin`, canonical RFC registration plus MSAL 5.4.2's `device_code` wire grant, pending/slow-down/declined/invalid/expired errors, trusted Graph overage, atomic one-winner consumption with token persistence, distinct access/ID-token `uti`, public refresh rotation, lifecycle rejection, and structural/raw/URI/form secret redaction. The public device-only application is registered with `redirectUris: []`; authorization-code or mixed applications still require a real callback URI. Fixtures 23–27 are implemented through the HTTP executor; mounted Worker covers creation, pending/slow-down, deny, approve/token/refresh/consume, and unknown code, not deterministic expiry. The actual-network result requires exactly 14 provider calls, one callback/pending poll, activation and device refresh `200`, post-disable `invalid_grant`, and combined token statuses `[200, 200, 200, 200, 400, 400, 400]`. Its one ownership-checked `/health` probe is a Wrangler-local liveness check outside provider evidence, not a token retry. MSAL 5.4.2 does not retry implemented `slow_down`. No remote Worker/Cloud, real Entra tenant, broad MSAL/device matrix, or P result exists. |
| IDP-OKTA-001 | Okta OIDC profile and errors | Complete for M2 slice locally; truthful UserInfo omission source-tested | 22 [source-reviewed fixtures](../packages/testkit/fixtures/okta/oidc), [core](../packages/core/src/okta.test.ts), [HTTP adapter](../packages/engine-http/src/okta.test.ts), Worker and MCP management tests. Current OIDC discovery and `get_wellknown_urls` omit UserInfo because no route exists; this adds no UserInfo support or evidence tier, and live-tenant and deployed-smoke comparison remain open. |
| IDP-OKTA-002 | Okta Classic primary authentication state machine | Partial M6; sampled deployed | Five executable [Authn fixtures](../packages/testkit/fixtures/okta/authn), the hash-only/expiry/replay/privacy [core suite](../packages/core/src/authn/okta-authn.test.ts), [HTTP adapter suite](../packages/engine-http/src/okta-authn.test.ts), and [Worker integration](../apps/worker/test/okta.integration.test.ts) cover `SUCCESS`, `MFA_REQUIRED`, `PASSWORD_EXPIRED`, explicit `LOCKED_OUT`, state retrieval/cancellation, and issuance-race denial. State retrieval slides a five-minute expiry; one-time session expiry is fixed at five minutes. Per-User/per-kind (32) and per-table (10,000) caps use oldest-expiry eviction, issuance GC is bounded to 256 expired rows per table, and operational indexes preserve schema v5. Lifecycle and SCIM password changes revoke capabilities. Same-origin CORS is restricted to `POST` and `accept`/`content-type`, without credentials; cross-origin is `403`. Responses use singular `_embedded.factor` and omit `passwordChanged`; Authn body/header secrets are recursively redacted. The [M6 deployed sample](./evidence/m6-workers-dev-smoke.md) exercised the initial states, state retrieval, privacy, CORS, and redaction. Factor verification, password-change execution, recovery, Sessions API exchange, unexercised source-only details, and verified-live comparison remain open. |
| IDP-OKTA-003 | Pinned Okta Auth JS public-client code + S256 PKCE, JWKS verification, refresh, revocation, lifecycle rejection, and bounded cleanup | Bounded local D/I/S/X/Q complete; H/V/P open | The shared [actual-network harness](../scripts/e2e-local-official-client.mjs), [official-client flow](../scripts/e2e-okta-authjs-client.mjs), [signal cleanup probe](../scripts/e2e-local-official-client-cleanup.mjs), and [local evidence](./evidence/okta-auth-js-local-qualification.md) pin `@okta/okta-auth-js` 8.0.1 and `@modelcontextprotocol/sdk` 1.29.0. MCP discovery/runtime create a public registration with no secret, observe the flow, apply `suspend`, and clean up. Auth JS uses returned discovery, `getWithRedirect`, S256 PKCE, `parseFromUrl` with the exact Node `_getLocation` seam, JWKS-backed ID-token verification, refresh rotation, and public access-token revocation over local Wrangler HTTPS. Lifecycle revokes exactly one remaining access token, proving the SDK revocation changed state, then Auth JS surfaces `invalid_grant`/`User account is disabled.`. A cross-client core regression protects owner isolation. The exact eight-request sequence, token statuses `[200, 200, 400]`, raw/URI/form-encoded value absence, named-field structural redaction, normal cleanup, and focused ready-Worker `SIGTERM` cleanup pass. Browser/other SDK versions, public introspection, UserInfo, device/Classic flows, hosted Cloud, real Okta, and P remain outside the requirement. |
| SCIM-001 | RFC 7643 resource schemas and discovery | Complete for M3 scope | Portable [contracts](../packages/contracts/src/scim.ts), discovery routes in the [SCIM adapter](../packages/engine-http/src/scim.ts), the [Worker mount](../apps/worker/test/scim.integration.test.ts), and the RFC fixture corpus cover ServiceProviderConfig, ResourceTypes, core User/Group schemas, and the enterprise User extension. Both deployed provider profiles passed ServiceProviderConfig discovery; no live-provider comparison is claimed |
| SCIM-002 | RFC 7644 CRUD, filter, PATCH, pagination, and ETag | Complete for the bounded M3 scope | Versioned [directory repositories](../packages/core/src/directory), bounded [filter](../packages/core/src/scim/filter.ts) and [PATCH](../packages/core/src/scim/patch.ts) code, adapter tests, and all 91 [RFC fixtures](../packages/testkit/fixtures/rfc/scim) execute green locally and in hosted CI. The deployed sample passed Group PATCH persistence and ETag checks for both profiles; it did not run all fixtures remotely |
| SCIM-003 | Entra and Okta inbound SCIM dialects | Complete for M3 scope | Provider profiles select case-insensitive and filtered Entra PATCH shapes with optional Group `204`, and Okta PUT-heavy/pathless/filtered membership shapes with Group `200`; all 10 [Entra](../packages/testkit/fixtures/entra/scim) and 12 [Okta](../packages/testkit/fixtures/okta/scim) fixtures execute green locally and in hosted CI, while deployed discovery/PATCH passed for both. Verified-live parity is not claimed |
| SCIM-004 | Deterministic conflict, delete-race, and narrow PATCH-tolerance edges | Partial M6; sampled deployed | The separate [M6 fixture corpus](../packages/testkit/fixtures/m6/scim), [scenario lock tests](../packages/core/src/scenario-log.test.ts), and [HTTP execution suite](../packages/engine-http/src/scim-edge-cases.test.ts) cover injected `409` rollback, soft-delete-race tombstone effects, strict rejection before compatibility repair, and exact `missing_schemas`/`singleton_operations` tolerances. The tolerances remain case-specific and do not repair combined or unrelated defects. The M6 smoke sampled the central conflict/race/strict/repair paths; a remote run of the complete eight-case corpus and verified-live comparison remain open |
| DIR-001 | Microsoft Graph directory reads | Complete for the bounded M3 read scope | The [Graph adapter](../packages/engine-http/src/graph.ts), [adapter tests](../packages/engine-http/src/graph.test.ts), portable [composition seam](../packages/worker-kit/src/directory-http.ts), and Worker integration cover bounded User/Group reads, membership reads, selection, filters, and cursor pages. The deployed smoke sampled a projected User read; Graph writes and broad parity are not claimed |
| DIR-002 | Okta Users, Groups, memberships, and lifecycle API | Complete for the bounded M3 scope | The [Okta directory adapter](../packages/engine-http/src/okta-api.ts), [adapter tests](../packages/engine-http/src/okta-api.test.ts), portable [composition seam](../packages/worker-kit/src/directory-http.ts), and Worker integration cover bounded Users, Groups, memberships, pages, and lifecycle actions. The deployed smoke sampled an active User read and `E0000047`; the separate bounded Classic Authn source candidate does not imply broad organization API parity. |
| LIFE-001 | Provider lifecycle state machine and credential/membership cascade | Complete for M3 scope; additive M6 and official-client source coverage | [Lifecycle policy](../packages/core/src/directory/lifecycle.ts), [core tests](../packages/core/src/directory-lifecycle.test.ts), and the [Worker cascade integration](../apps/worker/test/lifecycle-cascade.integration.test.ts) cover Entra activate/disable/reactivate/delete, Okta activate/reactivate/suspend/unsuspend/deprovision/delete, transactional access/refresh revocation, stable no-ops, and User-delete membership removal with affected Group version bumps. The bounded M6 [Authn core suite](../packages/core/src/authn/okta-authn.test.ts) additionally proves lifecycle revocation of pending state and one-time session capabilities across reactivation. Deployed raw-protocol acceptance sampled Entra disable, version advance, revocation, and AADSTS50057. Separately, pinned MSAL observes `invalid_grant` for both confidential-code and public-device refresh families after disable, with `AADSTS50057` asserted on the confidential path; pinned Auth JS observes `invalid_grant`/`User account is disabled.` after Okta suspend over local HTTPS. Both official-client results have no H/V/P evidence. |
| APP-001 | Create public/confidential application registrations and list secret-free metadata | Partial M7 plus bounded public-client source/X/Q; H/V/P open | [Contracts](../packages/contracts/src/index.ts), [core storage](../packages/core/src/directory/applications.ts), MCP tests, and the Auth JS/MSAL device flows distinguish confidential from public registrations. Confidential remains the default, stores only a hash, and returns a plaintext secret once; public stores and returns no secret and rejects both a supplied secret and `client_credentials`. SDK 1.29 `tools/list` exposes a strict input with optional defaults, explicit Draft-7 registration conditionals, and separate output branches with no secret property for public. A public device-only registration accepts `redirectUris: []`; any registration containing `authorization_code`, including a mixed code/device application, requires at least one real, exact callback URI. The strict application summary/page contracts, stable kind-bound keyset tests in [core](../packages/core/src/core.test.ts), and [Durable Object integration](../apps/worker/test/management-rpc.integration.test.ts) prove bounded 25-record pages containing neither `clientSecret` nor `secret_hash`. Public creation has local Auth JS and MSAL Node Q; neither registration type has new hosted or console one-time-display acceptance. |
| RBAC-001 | App roles and assignments | Partial | Storage contracts may exist; token-claim integration required |
| RBAC-002 | Group claims and overage | Partial M6; sampled deployed | [Token scenario tests](../packages/core/src/token-scenarios.test.ts), [Graph adapter tests](../packages/engine-http/src/graph.test.ts), [Worker integration](../apps/worker/test/token-edge-cases.integration.test.ts), and the [M6 fixture executor](../apps/worker/test/token-fixtures.integration.test.ts) cover the exact 200/201 boundary, a 201-ID-only token probe, trusted path/subdomain claim-source derivation, and a same-environment fallback capped at 1,000 returned IDs through a 1,001-ID-only probe. The deployed smoke exercised the path-mode 200/201 transition and exact 201-ID fallback; the ceiling, subdomain mode, and verified-live comparison remain source-only or open |
| MAP-001 | Entra/Okta profile mapping without engine forks | Complete for M3 scope | Shared engine provider profiles in [core](../packages/core/src/providers) select inbound SCIM PATCH/status behavior and provider lifecycle policy. The local/hosted suite is green, and deployed SCIM PATCH returned the configured Entra `204` and Okta `200`; verified-live qualification remains open |
| TOK-001 | RS256 signing and JWKS validation | Complete for M2 scope; M6 rollover sampled deployed | [Core crypto test](../packages/core/src/core.test.ts), [M6 key-ring tests](../packages/core/src/token-scenarios.test.ts), and Worker round trips cover signing, verification by `kid`, schema-v5-compatible metadata-only overlap, private-JWK scrubbing, the exact 26-hour gate qualified for fixed-lifetime, bounded-skew Worker/MCP issuance, stale-service cache refresh, and forced sign/rotate interleaving. Longer custom claims from trusted public-core test seams are outside that guarantee. The M6 deployed smoke exercised rotation plus stale/fresh JWKS verification; scrubbing, concurrency, and the full time gate remain source evidence |
| TOK-002 | Hashed rotating refresh-token families | Complete for M3 scope | [OAuth core](../packages/core/src/oauth/oauth-service.ts) and [tests](../packages/core/src/okta.test.ts) cover hashed tokens, atomic single-use rotation, absolute family expiry/auth-time preservation, scope narrowing, rollback on persistence failure, concurrent redemption, replay-family revocation, and associated access-token revocation. Entra and Okta Worker paths pass locally/hosted; deployed acceptance sampled Entra rotation, narrowing, signed identity, lifecycle revocation, and rejected reuse after disable |
| TOK-003 | Deterministic broken-token minting | Partial M6; sampled deployed | [Token scenario tests](../packages/core/src/token-scenarios.test.ts), the authenticated [Worker edge suite](../apps/worker/test/token-edge-cases.integration.test.ts), and Entra fixtures 31–35 cover `expired`, exact wrong audience, `not_yet_valid`, `bad_signature`, and exact wrong issuer. The deployed smoke minted and inspected all five variants. Neither source nor deployed mock evidence claims equivalent provider HTTP grants or verified-live parity |
| TOK-004 | Bounded claim-only clock skew | Partial M6; sampled deployed | The exact-only `token.before_sign` scenario and Entra fixture 37 shift temporal claims within plus/minus 86,400 seconds while leaving the environment clock and persisted authorization/device/refresh timestamps unchanged; the Worker executor and deployed smoke prove a plus-300-second one-shot signed token. Other bounds remain source evidence and verified-live comparison remains open |
| TEN-001 | One isolated SQLite store per environment | Complete for M2 | [Node adapter test](../packages/testkit/src/testkit.test.ts), Durable Object [MCP isolation integration](../apps/worker/test/mcp.integration.test.ts), and empty-catalog cleanup in the [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| TEN-002 | Derive issuer per request; never persist absolute issuer | Complete for M2 | [Core persistence invariant](../packages/core/src/core.test.ts), request-host assertions in Worker integrations, and distinct staging/production issuer verification in the [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| TEN-003 | Path and subdomain host resolution | Complete in unit scope; F1 and bounded F2 routes source-qualified locally | [Host-resolver tests](../packages/worker-kit/src/host-resolver.test.ts) cover trusted-header replacement, environment Graph derivation, exact mock-MCP classification, and exact OpenAI/Anthropic mock-LLM path/subdomain classification without identity issuer metadata; [MCP location tests](../packages/worker-kit/src/mcp-agent.test.ts) cover the Entra login-host issuer plus environment-host SCIM/Graph split. Path-mode mock-LLM Worker integrations prove environment isolation. Subdomain live TLS/routing verification remains M8 |
| SCN-001 | Deterministic delay/error/mutation injection | Complete for M2 | [Core deterministic/cap tests](../packages/core/src/scenario-log.test.ts), [Worker integration](../apps/worker/test/mcp.integration.test.ts), and one-shot live error injection in the [deployed smoke](./evidence/m2-workers-dev-smoke.md) |
| SCN-002 | List and manage persisted scenarios without consuming them | Partial M7; source-complete | Existing set/replace/clear behavior is joined by a strict, 25-record, kind-bound keyset page in the [scenario service](../packages/core/src/scenario/scenario-service.ts) and additive `listScenarios` Durable Object RPC. [Core tests](../packages/core/src/scenario-log.test.ts) prove listing does not advance evaluations and reflects current enabled/remaining state; [Worker integration](../apps/worker/test/management-rpc.integration.test.ts) covers list/replace/clear. MCP/HTTP/CLI list surfaces and deployed/private-console evidence remain outside this slice. |
| MCP-001 | Handler-agnostic tool registry | Complete for tested M5; eight-tool F1 bounded local D/I/S/X/Q; 27-tool current source registry | The [registry and tests](../packages/mcp/src/index.test.ts) preserve the accepted 15-tool set, append all eight F1 operations in canonical order (five server-definition/state operations followed by three built-in-blueprint operations), then append four F2 definition operations. F1 put/install require `expectedRevision: null` or a positive current revision; canonical identical replay, including identical delete/recreate convergence, resolves before CAS, while only changed stale/ABA intent conflicts. Reset/delete require a positive current revision. Changed put/install replacements are destructive because they delete prior application state and terminate revision-bound sessions. Bearer validation allows the raw credential only at `authentication.token`, rejects its exact value elsewhere, and fails closed on dependency reflection; install verifies the full public installed specification. The current registry is exactly 27 tools and five HTTP routes. Mounted Worker integrations use `@modelcontextprotocol/sdk` `1.29.0` to exercise custom-server and Salesforce-blueprint discovery, CAS, runtime, observation, and cleanup, qualifying only those bounded local paths through Q without claiming actual network. The SDK 1.29 tools-list regression separately verifies optional application defaults, strict unknown-field handling, the public no-secret/no-`client_credentials` conditional, exact output branches, and omitted-client-type compatibility. M5 registry, authenticated Worker/Workflow, full-gate, built-CLI process, and hosted staging/production start evidence are green only for their historical slice. The Entra and Okta client tranches separately use the same SDK over Streamable HTTP for named actual-network local Wrangler flows. Neither F-series addition inherits hosted/deployed/Cloud-pin or P evidence; V remains not applicable to the generic synthetic F1 engine but unqualified for the provider-derived Salesforce preset, and those client runs do not qualify every tool or a complete deployed command matrix. |
| MCP-002 | Session current-environment cursor | Complete for M2 | [Registry cursor tests](../packages/mcp/src/index.test.ts) and mounted-session [Worker integration](../apps/worker/test/mcp.integration.test.ts) |
| F0-CONTRACT-001 | Shared versioned behavior and management operation contracts | Source-complete locally | [Behavior schemas and tests](../packages/contracts/src/behavior.test.ts) cover all six locked version-one variants, seeded bounded latency, exact script references, fallback invariants, and explicit rejection of `proxy`. The [operation registry](../packages/contracts/src/operations/management.ts) exhaustively matches all 27 current tool names and supplies MCP metadata without changing the existing handler/cursor model. |
| F0-API-001 | One checked source for server metadata, OpenAPI, typed fetch client, and management reference | Source-complete locally | The five implemented self-hosted management HTTP routes consume registry paths/schemas in the [Worker](../apps/worker/src/app.ts). Deterministic generation produces the [OpenAPI 3.1 document](../packages/openapi/openapi/mockos-management.v1.json), [client operation map](../packages/client/src/generated.ts), generated [human tool reference](./reference/management-tools.md), [machine operation catalog](./reference/management-operations.v1.json), [built-in mock-MCP blueprint catalog](./reference/mock-mcp-blueprints.v1.json), [product capability index](./reference/product-capabilities.v1.json), and separate [mock OpenAI](./reference/mock-llm-openai.v1.json) and [mock Anthropic](./reference/mock-llm-anthropic.v1.json) provider manifests; root/CI drift checks compare them byte-for-byte. The catalogs record all eight F1 operations as MCP-only with exact null-create/positive-replace put/install intent, canonical-before-CAS replay including identical delete/recreate convergence, changed-only stale/ABA conflicts, destructive changed replacement, Bearer-credential containment, dependency-reflection failure, full public blueprint-install spec matching, positive reset/delete CAS, preserved reset state boundaries, literal delete success, and typed missing/conflict outcomes. The capability index is explicitly a non-exhaustive F0-F2 interface slice: it covers management MCP, companion HTTP, mock-MCP/OpenAI/Anthropic data planes, and disabled Code Mode while naming identity-provider, SCIM/provisioning-outcome, and private Cloud product rows as unindexed. Existing management operations for those public behaviors remain covered by the MCP/HTTP rows; public provider/outcome rows await exact contracts, while private Cloud remains a private revision-pinned overlay. Each included row links executable authorities, an exact specification, guides, anchored limitations, and independent evidence claims. Qualification, coverage, and current-versus-historical revision relation are separate, and unqualified context cannot serve as proof. Each provider manifest derives from its executable operation contract and does not expand management OpenAPI. [OpenAPI tests](../packages/openapi/test/drift.test.ts) require exactly 27 tool IDs, five management HTTP routes, exact F1/F2 source/deployment labels, exact F1 mutation and blueprint schemas, exact bounded provider operations/limits, and inert secret placeholders. Documentation checks resolve capability paths, JSON Pointers, Markdown anchors, and named TypeScript exports; require proof only for qualified claims; retain separate context for unqualified boundaries; and scan generated artifacts for secret-shaped values. [Client tests](../packages/client/test/client.test.ts) cover encoding, constructor-owned authorization, response/problem validation, aborts, and unexpected protocol shapes. All eight F1 and all four F2 management operations remain MCP-only. |
| F0-RUNTIME-001 | Experimental wrappers and exact pins remain fail-closed | Complete for the F0 source boundary | `@cloudflare/codemode` `0.4.3`, `agents` `0.17.4`, MCP SDK `1.29.0`, and Wrangler `4.114.0` are exact catalog pins checked against the direct wrapper importer. [Code Mode tests](../packages/codemode/test/disabled.test.ts) prove the upstream factory is untouched without literal enablement, and [`NoSandbox` tests](../packages/sandbox/test/no-sandbox.test.ts) prove source/input are not evaluated. Guard checks forbid wrapper imports, `LOADER`, `worker_loaders`, or enable flags in the Worker runtime. |
| MCPMOCK-001 | Strict, bounded environment-local mock MCP definition and management | Complete for bounded F1 local D/I/S/X/Q | [Contracts and tests](../packages/contracts/src/mock-mcp.test.ts) cover 64-server/64-capability bounds, safe read views, write-only Bearer Mock Credentials, bounded schema/result/spec trees, safe Level-1 resource templates, exact configured error maps that exclude reserved `-32050`, and five server-definition/state management schemas. The [blueprint contract](../packages/contracts/src/mock-mcp-blueprint.ts) adds three secret-free built-in-catalog/install schemas with explicit provenance and fidelity. Put/install require strict `expectedRevision: null` create or positive-current-replace intent; reset/delete require a positive safe integer. Unknown top-level put arguments and nested definition failures collapse to secret-safe generic issues, safe Bearer `configured: true` views are not replacement inputs, a raw token is accepted only at `authentication.token`, and its exact value is rejected from every other definition key or string value. [Management registry tests](../packages/contracts/src/operations/management.test.ts) and [handler tests](../packages/mcp/src/index.test.ts) preserve all eight F1 tools inside the 27-tool current registry while HTTP remains five, keep discovery/runtime schemas identical, cover typed failures, fail closed on dependency credential reflection, and require blueprint install to match the complete public server spec. The mounted official SDK `1.29.0` Worker flows qualify only these bounded local management paths through Q; actual-network, hosted, P, and deployed evidence remain open. V is not applicable to the generic synthetic engine and remains unqualified for the provider-derived Salesforce preset. |
| MCPMOCK-002 | Hand-written MCP `2025-11-25` Streamable HTTP adapter | Complete for bounded F1 local source | [`@mockos/mcp-mock`](../packages/mcp-mock) owns raw parsing/rendering, initialization, protocol/session headers, POST/DELETE lifecycle, GET `405`, method dispatch, pagination, schema validation, configured errors, and HTTP-Fetch-abort protection during configured latency. [Schema-validator tests](../packages/mcp-mock/src/json-schema.test.ts) prove one sticky work budget across composition, traversal, long-parent path construction, uniqueness, and canonical comparisons, plus bounded HTTP-`200` tool-error mapping with one root diagnostic. Raw-wire coverage proves `notifications/cancelled` is accepted and deliberately ignored because F1 has no message-level in-flight cancellation registry. Raw-wire, in-process official-client, [routed Worker official-client](../apps/worker/test/mock-mcp.integration.test.ts), and complete local repository gates are green source evidence; GET streaming, `listChanged`, batches, a second version adapter, and message-level cancellation are explicitly out of scope. |
| MCPMOCK-003 | Deterministic tools, resources, templates, prompts, and behavior | Complete for bounded F1 local source | The [behavior evaluator](../packages/core/src/behavior/evaluator.ts) and adapter cover static/template/match/sequence/configured-error behavior, method-specific result validation, deterministic latency, and expected-state commit. [Resource-template tests](../packages/mcp-mock/src/resource-template.test.ts) prove bounded/linear reverse matching, empty and percent-decoded expansions, and leftmost-minimal adjacent captures. [Core behavior tests](../packages/core/src/behavior/evaluator.test.ts) and [foundation regression](../packages/core/src/mock-mcp/foundation.test.ts) prove request-local canonical/path/UTF-8 reuse, a 100-case match over a 12,000-property value without repeated traversal/canonicalization, incremental template overflow before append, 200 repeated large placeholders, and exact/over-limit Unicode byte boundaries. Scripts use an injected executor only and therefore fall back or fail closed in F1; `proxy` is rejected. |
| MCPMOCK-004 | Revision-bound mutation/state and hash-only transport sessions | Complete for bounded F1 local source | The [repository](../packages/core/src/mock-mcp/repository.ts), [capacity/foundation tests](../packages/core/src/mock-mcp/foundation.test.ts), [session tests](../packages/core/src/mock-mcp/sessions.test.ts), [adapter/repository tests](../packages/mcp-mock/src/repository-integration.test.ts), and mounted [custom-server](../apps/worker/test/mock-mcp.integration.test.ts) and [blueprint](../apps/worker/test/mock-mcp-blueprint.integration.test.ts) Worker integrations cover null-create/positive-replace CAS, canonical replay before CAS including identical delete/recreate convergence, one-winner concurrent replacement, changed stale/ABA denial, a one-row monotonic safe-integer revision allocator that survives complete deletion, changed-definition state/session invalidation for both put and install, reset at the current revision with exact `cleared: 0` replay, atomic current-revision delete with literal `deleted: true` and typed not-found replay, explicit child deletion without relying on foreign-key enforcement, per-value limits, a 256-row/2-MiB aggregate state ceiling with atomic sequential/concurrent failure mapped to HTTP `200`/JSON-RPC `-32050`, 32-byte opaque session issuance with hash-only persistence, slug/revision/version/TTL binding, bounded pruning, and a 100-active-session cap. Rejected mutations do not allocate revisions or touch replacement state/sessions. Hosted durability/load evidence remains open. |
| MCPMOCK-005 | Exact environment routing and credential separation | Complete for bounded F1 local source | [Host-resolver tests](../packages/worker-kit/src/host-resolver.test.ts) cover `/e/{environmentId}/mcp-mock/{slug}`, the environment subdomain form, malformed/nested route denial, and trusted internal-header replacement. The [path-mode Worker round trip](../apps/worker/test/mock-mcp.integration.test.ts) proves routed EnvironmentDO dispatch and rejects both the active management credential and a wrong mock Bearer value. Wildcard TLS, custom-domain, hosted Cloud, and production security qualification remain open. |
| MCPMOCK-006 | MCP-aware request observation and assertions | Complete for bounded F1 local source | [Request-log contracts](../packages/contracts/src/index.ts) and [core tests](../packages/core/src/scenario-log.test.ts) cover protocol/method/tool/canonical-arguments, JSON-RPC error code including `-32800`, tool `isError`, filters, exact matches, and ordered cross-source sequences. Adapter tests prove bounded recursive argument redaction and no raw credential/session disclosure; the [Worker round trip](../apps/worker/test/mock-mcp.integration.test.ts) queries the management request log for exact method/tool/arguments, verifies an unsupported top-level argument-key length collapses to a fixed marker, and proves its mock token and session ID are absent. |
| MCPMOCK-007 | Built-in Salesforce Hosted MCP SObject Reads server-definition preset | Complete for bounded local D/I/S/X/Q; H/V/P unqualified | The schema-valid [built-in catalog](../packages/core/src/mock-mcp/blueprints.ts), [core fixture tests](../packages/core/src/mock-mcp/blueprints.test.ts), and [mounted Worker integration](../apps/worker/test/mock-mcp-blueprint.integration.test.ts) list/get/install `salesforce/hosted-mcp/sobject-reads`, then discover and exercise the six ordered tools `getObjectSchema`, `soqlQuery`, `find`, `getUserInfo`, `listRecentSobjectRecords`, and `getRelatedRecords` through official MCP SDK `1.29.0`, observe/assert the calls, and clean up. Provenance records Salesforce Hosted MCP SObject Reads documentation reviewed on 2026-07-25. That source review is design input, not V evidence. The preset is stateless, credential-free, deterministic, and limited to exact `mockos.test` fixtures; it contacts no Salesforce network and does not qualify REST, OAuth, field-level security, sharing, or output-wire parity. It is a built-in server-definition preset, not the portable F5 export/import/apply/gallery system. The [human guide](./blueprints/salesforce-sobject-reads.md) and [machine catalog](./reference/mock-mcp-blueprints.v1.json) own its exact inputs, fixtures, provenance, and evidence. Actual-network, hosted, deployed, Cloud-pin, H, V, and P evidence remain unqualified. |
| LLMMOCK-001 | Provider-neutral response plan, server definition, and behavior compatibility | Partial F2 source implementation | The [F2 kernel record](./f-series/f2-llm-kernel.md) maps bounded/deep-frozen response plans and the credential-free normalized-request planner seam. The strict [server-definition contract](../packages/contracts/src/mock-llm-server.ts) binds provider dialects, 1–64 models, shared behaviors, usage/cadence defaults, write-only strict credentials, and safe views. Focused tests cover behavior-to-plan conversion, stateless turn-index selection, validation-before-explicit-commit, stable hashes/IDs, definition bounds, provider-key non-reflection, secret-safe strict top-level input rejection, and bounded neutral-plan validation for static definitions. No response/conversation-state owner exists; the bounded provider composition is traced separately in LLMMOCK-004. |
| LLMMOCK-002 | OpenAI and Anthropic pure dialect rendering | Partial F2 focused source evidence | The [`@mockos/llm-mock`](../packages/llm-mock) package renders selected OpenAI Chat Completions and Anthropic Messages success/error JSON plus complete text/tool/usage SSE-frame sequences from the same neutral plan. Both renderers distinguish payload deltas from immediate structural and terminal frames. Pure renderer tests freeze those projections; pinned official clients consume selected HTTP-handler responses through injected Fetch. Pure tests do not by themselves establish timed delivery, backpressure, cancellation, broad SDK, deployed, or verified-live conformance. Separate edge-stream package tests establish pacing/deadline/backpressure behavior, and both HTTP adapters/runtimes compose that helper in LLMMOCK-004. |
| LLMMOCK-003 | MCP-managed environment-local mock LLM definitions | Partial F2 source implementation | Four MCP-only operations source-implement create/list/get/delete over an environment-local schema-v8 repository. Changed definitions require the current revision and full resupply/rotation of every enabled strict provider key; canonical replay is idempotent, while safe `configured` markers are read-only. Delete requires positive revision CAS, returns typed `409` for stale/ABA state, and `deleted: false` when absent/retried. Provider keys are hashed before storage; bounded definition keys/string values containing the platform key as a substring are rejected twice. The [task guide](./mock-llm.md) maps focused contract/core/MCP/Worker evidence, v7-to-v8 observation upgrade, and explicit older-v7/newer-v8 refusal/forward-recovery warning. Management remains MCP-only with no additional `__mockos/v1` route. |
| LLMMOCK-004 | Environment-hosted OpenAI and Anthropic streaming provider data planes | Partial F2; bounded local source qualification | Executable [OpenAI](../packages/llm-mock/src/openai-http.ts) and [Anthropic](../packages/llm-mock/src/anthropic-http.ts) adapters plus generated [OpenAI](./reference/mock-llm-openai.v1.json) and [Anthropic](./reference/mock-llm-anthropic.v1.json) manifests own exact path/subdomain bases, model list/retrieve, Chat Completions/Messages as JSON or bounded SSE, strict request/tool/response limits, provider-shaped errors, fresh IDs, and declared-tool enforcement. OpenAI accepts `stream: true` and only the optional Boolean `stream_options.include_usage` and `stream_options.include_obfuscation`; stream options fail when streaming is off and unknown keys fail closed. Obfuscation defaults on with fresh opaque compatibility padding on regular delta chunks, without an upstream size-normalization or security-parity claim. Anthropic accepts Boolean `stream: true`, requires `x-api-key` and exactly `anthropic-version: 2023-06-01`, and rejects beta/alternate-auth headers. Its named events have zero or more payload deltas per content block, cumulative `message_delta` usage, and a terminal `message_stop`, with no `[DONE]` or mock-emitted `ping`; clients should tolerate upstream `ping`. Shared edge-stream package tests prove initial delay before headers, payload-only pacing, immediate structural/terminal frames, one absolute maximum including initial wait, pacing, and backpressure, and post-`200` cancellation/deadline truncation without fabricated success. The complete precomputed SSE body is capped at 2,097,152 UTF-8 bytes. Preflight requires `initialDelayMilliseconds + Math.max(payloadFrameCount - 1, 0) * chunkDelayMilliseconds` to be strictly less than `maximumDurationMilliseconds`; equality is rejected, and payload frame count comes from Unicode code-point chunks of text and canonical provider tool arguments/input JSON. Other preflight failures return generic JSON before HTTP `200`. Configured errors also remain provider JSON before `200` even for stream requests; configured midstream errors are unsupported. The [environment runtime](../packages/worker-kit/src/mock-llm-runtime.ts) requires a syntactically valid dialect-channel credential even for `accept_any`, hashes before current-definition selection, compares strict verifiers without early exit, always rejects the platform key and alternate auth channels, rejects body reflection, derives stateless turns from prior assistant messages, and rechecks the definition revision before committing the plan in the Environment Durable Object and returning it to the edge. The current state adapter discards staged writes; post-header cancellation does not roll back that completed commit, and persisted state requires a separately qualified design. Only `strict` distinguishes a wrong or other-dialect credential value; `accept_any` accepts any syntactically valid Mock Credential through the required channel. [OpenAI](../apps/worker/test/mock-llm.integration.test.ts) and [Anthropic](../apps/worker/test/mock-llm-anthropic.integration.test.ts) local Worker integrations configure through MCP and exercise pinned official SDKs, models, text, tools, usage, errors, isolation, rotation, streams, cancellation, and secret-safe observations. Worker fixtures use zero configured chunk delay, so exact timing/backpressure evidence remains in the shared scheduler tests. Anthropic betas, configured midstream errors, the Responses API, conversation/evaluator state, reset, Wrangler-network, hosted CI, Cloud pinning, deployment, production readiness, and live-provider parity remain unavailable or unqualified. |
| LLMMOCK-005 | Metadata-only OpenAI/Anthropic request observation and exact assertions | Complete for the bounded F2 local source slice | [Contracts](../packages/contracts/src/index.ts), [request-log persistence](../packages/core/src/log/request-log.ts), provider observation hooks, and Worker-kit composition reserve one `pending` row only after a Chat Completions/Messages POST is successfully parsed/planned and response-preflighted, then append-once overlay `completed`, `cancelled`, `deadline_exceeded`, or `failed` status/duration on that same append sequence. Reservation wait is capped at 50 milliseconds; storage failure, timeout, or a credential collision in any prospective durable metadata leaves the provider response unchanged. A non-cancellable timed-out hook can still create a late `pending` row. Actual delivered status/monotonic duration enter only the terminal child; pending reads expose explicit `102`/`0` compatibility sentinels for legacy non-null columns. The exact plan-selection server revision is retained. Response metadata stores a preallocated response ID plus usage/stop/ordered tool names. Configured errors omit response ID, store `llmErrorKind`, and finalize `completed` when delivered; `llmStream` records accepted request intent even when the error is pre-header JSON. Existing `get_request_log` and `assert_requests` schemas exact-match every LLM field, including false/zero and complete ordered tool-name arrays, and preserve greedy-earliest non-overlapping ordered sequences. Entries always contain empty request/response headers and null bodies; prompts, outputs, credentials, tool inputs, `planId`, and `requestHash` never enter the contract. A request-local privacy guard covers event fields, routed path, exact revision, pending sentinels, every terminal outcome, status, and duration; a collision skips the whole observation. Internal edge frame/byte counts are not persisted or queryable. Reservation/finalization are fail-open, so evidence can be absent or remain pending and is not audit-grade. Contract/core/provider/edge-router tests plus mounted Worker/MCP integration qualify designed, implemented, source-tested, and integration-tested for this bounded slice. Pinned provider SDKs qualify provider behavior; observation query/assertion uses the mounted official MCP client. Direct edge/provider tests own persisted cancellation evidence because the Worker-pool binding does not propagate stream reader cancellation. Actual-network, hosted-smoke, verified-live, and production-ready remain unqualified. |
| AUDIT-001 | Inbound/outbound request log and assertions | Complete for tested M5; F1 MCP and bounded F2 LLM metadata source-qualified locally | The engine supports inbound/outbound entries, filtering, pagination, retention, response-body predicates, repeated non-overlapping ordered assertions, F1 MCP method/tool/argument metadata, and bounded F2 LLM lifecycle metadata. Local process evidence and both M5 hosted runs matched the expected four-request sequence; F1/F2 metadata additions remain source-only, F2 is deliberately fail-open, and neither is a broad live-provider or audit-completeness claim. |
| AUDIT-002 | Redact platform, target, and structured authentication secrets while retaining safe synthetic protocol evidence | Complete for tested M5; M6 Authn H plus MSAL/Auth JS local S/X/Q added | [Outbound HTTP tests](../packages/worker-kit/src/provisioning-http.test.ts), Worker tests, and process e2e cover credential scoping/redaction and active-key confusion denial. Manual M5 acceptance retained no target credentials and left target state/capture empty. The M6 Authn Worker integration proves password/state/session-token body redaction, including malformed JSON; the deployed smoke sampled exact header/body redaction and safe retained evidence. Focused Worker coverage plus the MSAL and Auth JS actual-network flows protect structured OAuth/device fields, token responses, and redirect `Location` secrets. Each official-client flow parses named exercised fields as `[REDACTED]` and rejects raw, `encodeURIComponent`, and URL-form-encoded representations. The combined MSAL path adds device/user codes, the repeated device message, activation credentials, device polling, and successful device token/refresh responses while retaining the safe decision and exact sequence. Focused Worker coverage also replaces malformed form/JSON, primitive JSON request, and unsupported-media request bodies. This is exact-value/encoding and key/media-type evidence, not arbitrary-encoding classification, a general secret audit, or P evidence. |
| PROV-001 | Deterministic outbound SCIM planner and response interpreter | Complete for the tested M5 slice | The portable [contracts](../packages/contracts/src/provisioning.ts) and [core planner/interpreter](../packages/core/src/provisioning) cover provider-specific shapes, watermarks, lookups, and explicit rate-limit waits/retries. Focused/full gates and the controlled Entra-shaped hosted flows passed; live-provider parity remains open |
| PROV-002 | SSRF guard at save and fetch time | Complete for the tested M5 boundary | [Secure fetch](../packages/worker-kit/src/secure-fetch.ts) validates ingress/fetch, requires HTTPS by default, blocks private/special literal and product hosts, disables redirects, and bounds time/bodies. Controlled public-HTTPS target execution passed. Workers still cannot pin DNS answers; see known limitations |
| PROV-003 | Durable provisioning-cycle execution and active-start reconciliation | Complete for the tested M5 slice | The [Workflow entrypoint](../packages/worker-kit/src/provisioning-workflow.ts), Durable Object persistence/isolation, batching, ordinals, watermarks, fixed run IDs, and recovery tests are green. Exact staging and production runs reached platform `complete` and runtime `succeeded`. Request-level terminal replay remains deferred to F4 |
| PROV-004 | Deterministic SCIM target test surface and sequence assertion | Complete for the tested M5 slice | The [target app](../examples/target-app) and ordered assertion passed locally and in hosted staging/production with four matched requests. The disposable target ended empty, was deleted, and returned `404`; direct loopback and live-provider comparison remain outside acceptance |
| OPS-001 | CI format/types/test/build + Wrangler gates | M6 hosted evidence accepted; F0/F1 source-qualified locally | Exact public revision `a01fb6abbaf85e2cd98b42a3839bebe7451cf8da` passed the full local gate and [hosted CI run 29966667984](https://github.com/reachjalil/mockos/actions/runs/29966667984). The F0/F1 revision carrying this document passes the expanded local gate with generated-artifact, disabled-runtime, mock-MCP conformance, Worker-routing, and Wrangler checks; hosted CI for that revision remains pending. |
| OPS-002 | workers.dev deploy and smoke | M6 exact-version sampled acceptance green; guarded path unqualified | Public staging version `9ea22805-e38e-4a1b-807f-f80646cbe298` and production version `0695adab-d162-4f01-a3cf-da9c1640acdc` were manually rolled out, confirmed 100% active, and passed source-locked smoke with the existing Access Keys preserved. The separate guarded Cloudflare-credential deployment workflow was not executed or formally qualified |
| CLI-001 | Non-interactive operator loop | Complete through M3; partial M5 provisioning command | Profiles, diagnostics, environment lifecycle, seeding, applications, scenarios, minting, log/assertion output, JUnit, well-known URLs, reports, and capability negotiation pass accepted source tests. The M5 `provision run` candidate accepts target secrets from a file or standard input, not argv; all 19 [CLI tests](../packages/cli/test/cli.test.ts) and the built-CLI process e2e are green locally. It rejects `mk_` keys and an exact active non-prefixed self-host Access Key before calling MCP. Publication and deployed command-matrix evidence remain open |
| OPS-003 | Custom-domain cutover | Blocked | Domain purchase and M8 runbook execution |
| SAML-001 | SAML protocol | Intentionally deferred | v2 product scope |
